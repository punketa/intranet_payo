sudo mysql

CREATE DATABASE intranet CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE USER 'inadmin'@'localhost' IDENTIFIED BY 'Admin123';

GRANT ALL PRIVILEGES ON intranet.* TO 'inadmin'@'localhost';

FLUSH PRIVILEGES;

USE intranet;

CREATE TABLE erabiltzaileak(
    id INT AUTO_INCREMENT PRIMARY KEY,
    erabiltzailea VARCHAR(50) NOT NULL UNIQUE,
    email VARCHAR(100) NOT NULL UNIQUE,
    pasahitza VARCHAR(255) NOT NULL,
    sortuta TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

EXIT;
